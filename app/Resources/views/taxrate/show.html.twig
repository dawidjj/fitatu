{% extends 'base.html.twig' %}

{% block body %}
    <h1>Taxrate</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taxRate.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ taxRate.value }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('taxrate_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('taxrate_edit', { 'id': taxRate.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
